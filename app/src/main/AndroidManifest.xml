<?xml version="1.0" encoding="utf-8"?>
<!--
    AndroidManifest.xml
    Declares the minimum permissions necessary for the app to function:
      - ACCESS_FINE_LOCATION: required for GPS fix (mandatory)
      - BLUETOOTH_SCAN / BLUETOOTH_CONNECT: BLE heart-rate monitor (Android 12+)
      - BLUETOOTH / BLUETOOTH_ADMIN: BLE on Android < 12
      - FOREGROUND_SERVICE: keep GPS + recording alive in background
      - FOREGROUND_SERVICE_LOCATION: required sub-type for location foreground services (API 34+)
    No WRITE_EXTERNAL_STORAGE is requested; TCX files are written to app-specific
    external storage (getExternalFilesDir) which requires no runtime permission.

    INTERNET / ACCESS_NETWORK_STATE are normal (not runtime) permissions used
    only by the map's optional "OpenStreetMap tiles" background preference to
    download map tiles; the default "plain black" background performs no
    network access at all.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Location: mandatory for GPS -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Map view: only used when the OSM tile background preference is selected -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Background location for recording while screen is off -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- Foreground service to keep GPS and recording running -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />

    <!-- BLE – Android 12+ (API 31+) -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- BLE – Android < 12 -->
    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />

    <!-- Hardware features (not permissions, used to declare requirements) -->
    <uses-feature android:name="android.hardware.location.gps" android:required="true" />
    <uses-feature android:name="android.hardware.sensor.compass" android:required="true" />
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="false" />

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GPSWaypoint">

        <!-- Main navigation activity -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Preferences screen: units and heart-rate monitor selection -->
        <activity
            android:name=".ui.PreferencesActivity"
            android:exported="false"
            android:label="@string/preferences_title"
            android:parentActivityName=".ui.MainActivity" />

        <!-- Map screen: planned route, elapsed route, and current location -->
        <activity
            android:name=".ui.MapActivity"
            android:exported="false"
            android:label="@string/map_title"
            android:parentActivityName=".ui.MainActivity" />

        <!-- Foreground service that manages GPS location updates and recording -->
        <service
            android:name=".service.NavigationService"
            android:foregroundServiceType="location"
            android:exported="false" />

    </application>
</manifest>
